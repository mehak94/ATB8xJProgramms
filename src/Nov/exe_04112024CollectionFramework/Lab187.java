package Nov.exe_04112024CollectionFramework;

public class Lab187 {
    public static void main(String[] args) {
// Hashing mechanism means to give unique identity to every element so that we can find then=m in memory easily.
        // LinkedList mechanism follows doubly method.
        // Red and tree mechanism means we can sort elements.
        // We can store null in hashset and linkedhashset but not in treeset
        // There is no indexes in case of Set but incase of List we have index.
    }
}
