package Nov.exe_18112024;

public class Lab209_HTMLReport {
    public static void main(String[] args) {
        // How we can generate HTML report?
        // 1) Download and Install the NODEJS
        // 2) In your CMD TYPE -> node --version, it will show us the version we have
        // 3) npm --version, it will tell us the version of npm
        // npm -g install newman
        // npm -g install newman-reporter-htmlextra
        //cd Postman,mkdir PostmanExp,cd PostmanExp
        // newman run mention URL then for generating report type -r cli,it means i can run the command line
        // then again type newman run mention URL -r cli,htmlextra , this will create a file
        // then type dir
        // cd newman
    }
}
