package oct.exe_21102024Wrapper.Exceptions;

public class Lab152 {
    public static void main(String[] args) {
        System.out.println("Start the program");
        String ip = args[0];
        System.out.println(ip);

        // Steps to get the value of argument :-
        // 1) Go to lab option then edit configuration
        // 2) Click on + sign then click on application
        // 3) enter lab and select class, enter value of CLI argument then apply.

        // THE ABOVE IS KNOWN AS TAKING ARGUMENT FROM THE COMMAND LINE.
        // We can take input in case of Arguments(String) by using CLI( Command line interface).
        // We can give multiple inputs at a time by using it to String[] this is known as string array.
    }
}
